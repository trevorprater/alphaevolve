Hello Claude, let's continue with AlphaEvolve.

**Role:** You are an expert Python developer.
**Task:** Implement core data structures for the Program Database, specifically `ProgramEntry` and `MAPElitesArchive`.

**File:** `alpha_evolve/program_database.py`

**Request:**

1. Define a dataclass (or Pydantic model) named `ProgramEntry` with fields:
    - `id: str` (unique identifier, e.g., using `uuid.uuid4().hex`)
    - `code: str` (the program's source code)
    - `scores: dict[str, float]` (metric names to float values)
    - `features: tuple` (phenotypic features for MAP-Elites, e.g., `(len(code), execution_time_proxy)`)
    - `parent_id: Optional[str] = None`
    - `generation: int`
2. Define a class `MAPElitesArchive`.
    - In its `__init__`, it should take `feature_dimensions_bins: list[list[Any]]`. Each inner list represents the discrete bins for a feature dimension. For example, `[[0, 10, 20], [0.1, 0.5, 1.0]]` could mean the first feature is binned by `<10, 10-19, >=20` and the second by `<0.1, 0.1-0.49, >=0.5`. The archive will store elites in a multi-dimensional dictionary or a similar structure.
    - Implement `_get_feature_bin_key(self, features: tuple) -> Optional[tuple[int, ...]]`: This private helper method should take a program's raw features tuple and map it to a tuple of bin indices based on `feature_dimensions_bins`. If features fall out of defined bins, it can return None or handle as per a defined strategy (e.g., clamp to nearest bin).
    - Implement `add_program(self, program_entry: ProgramEntry, primary_score_key: str) -> bool`:
      - Get the bin key for `program_entry.features` using `_get_feature_bin_key`.
      - If the key is valid:
        - If the corresponding cell in the archive is empty, store `program_entry` and return `True`.
        - If the cell is occupied, compare `program_entry.scores[primary_score_key]` with the stored elite's score. If the new program is better, replace the old one and return `True`.
        - Otherwise, return `False`.
      - If the key is invalid (e.g., out of bounds), return `False`.
    - Implement `get_elite(self, features: tuple) -> Optional[ProgramEntry]`: Returns the elite for the given features if one exists.
    - Implement `get_random_elites(self, count: int) -> list[ProgramEntry]`: Returns `count` random elites from the archive. Ensure it handles cases where the archive has fewer than `count` elites.

Please include clear docstrings, type hints, and basic error handling (e.g., for `KeyError` if `primary_score_key` is missing).
