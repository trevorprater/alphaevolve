Okay Claude, let's continue with `alpha_evolve/program_database.py`.
We have `ProgramEntry` and `MAPElitesArchive`. Now, let's implement the main `ProgramDatabase` class that uses them.

**Role:** You are an expert Python developer.
**Task:** Implement the `ProgramDatabase` class.

**File:** `alpha_evolve/program_database.py` (append to existing content)

**Request:**
Define the `ProgramDatabase` class:

- In its `__init__`, it should initialize a `MAPElitesArchive` instance (it will need `feature_dimensions_bins` and a `primary_score_key` passed to its constructor, or have defaults). It should also have a way to store all programs by ID (e.g., a dictionary `self.all_programs_by_id: dict[str, ProgramEntry]`).
- Implement `add_program(self, program_entry: ProgramEntry)`:
  - Store the `program_entry` in `self.all_programs_by_id`.
  - Attempt to add it to the `self.map_elites_archive` using its `add_program` method.
- Implement `get_program_by_id(self, program_id: str) -> Optional[ProgramEntry]`.
- Implement `sample_programs_for_prompting(self, num_parents: int, num_inspirations: int) -> tuple[list[ProgramEntry], list[ProgramEntry]]`:
  - This method should select programs from the `MAPElitesArchive`.
  - For now, both parents and inspirations can be sampled using `self.map_elites_archive.get_random_elites()`.
  - Ensure it handles cases where not enough distinct programs are available. Parents and inspirations should ideally be distinct from each other if possible, but this can be simple for now.
- **(Conceptual for Island Model - placeholder for now):** Add a method signature `trigger_migration(self, other_db: 'ProgramDatabase', num_to_migrate: int)` but leave its implementation as `pass` or a simple print statement. We will not implement full island logic yet.

Ensure clear docstrings and type hints.
