Thanks, Claude! Here's the `EvaluationEngine` code from `alpha_evolve/evaluation_engine.py`:

{{ Paste evaluation_engine.py code here }}

And a reminder of `EvaluationWrapper` from `task_utils.py` which it might use:

{{ Paste EvaluationWrapper class definition here }}

**Role:** You are an expert Python test engineer.
**Task:** Write unit tests for the `EvaluationEngine` class using `pytest`.

**File:** `tests/test_evaluation_engine.py`

**Request:**
Generate `pytest` unit tests for `EvaluationEngine.evaluate_program`:

1. **Successful Evaluation:**
    - Provide a `program_code_string` (e.g., a simple function definition `def solution_function_to_evaluate(): return 42`).
    - Provide a mock `user_evaluate_fn` that:
      - Takes the program string (or a mock object representing the executed code).
      - Returns a valid score dictionary (e.g., `{'accuracy': 0.95}`).
    - Assert that `evaluate_program` returns this score dictionary.
2. **Evaluation Function Error:**
    - Provide a mock `user_evaluate_fn` that raises an exception.
    - Assert that `evaluate_program` catches this and returns the special error score dictionary (e.g., `{'error': True, ...}`).
3. **Invalid Program Code String (Syntax Error):**
    - Provide a `program_code_string` with a Python syntax error.
    - If your `EvaluationEngine` attempts to `exec` or import this, it should handle the `SyntaxError` (or similar) and return the error score dictionary. This test might be tricky depending on how deep the execution logic goes in the current simplified version. Test the intended behavior.
4. **Program Runtime Error:**
    - Provide a `program_code_string` that is syntactically valid but will raise a runtime error when called (e.g., `def solution_function_to_evaluate(): return 1/0`).
    - Provide a `user_evaluate_fn` that calls this function.
    - Assert that `evaluate_program` handles this and returns the error score dictionary.

Ensure tests are `async` if testing an `async` method. Use `pytest.mark.asyncio`.
You'll need to define simple mock callables for `user_evaluate_fn`.
