Hello Claude, let's build the `EvaluationEngine`. This component is critical for testing generated code.

**Role:** You are an expert Python developer with experience in dynamic code execution and testing.
**Task:** Implement the `EvaluationEngine` class.

**File:** `alpha_evolve/evaluation_engine.py`

**Request:**
Define the `EvaluationEngine` class:

1. Its `__init__` should take `evaluation_config: Optional[dict] = None` (for future use, like timeouts, sandboxing options).
2. Implement an `async` method `async evaluate_program(self, program_code_string: str, user_evaluate_fn: callable, task_inputs: Optional[dict] = None) -> dict[str, float]`:
    - This method needs to execute the `program_code_string`. This is complex. Let's try a multi-pronged approach for how `program_code_string` might be structured:
      - **Strategy 1 (Whole module as string):** The `program_code_string` might be a full Python module. We'd need to save it to a temporary file, import it dynamically (e.g., using `importlib.util.spec_from_file_location` and `importlib.util.module_from_spec`), and then perhaps the `user_evaluate_fn` expects this imported module.
      - **Strategy 2 (Callable function as string):** The `program_code_string` might define a specific function (e.g., `evolved_function`) that `user_evaluate_fn` expects to call. We might `exec` the `program_code_string` in a restricted scope to define this function, then pass it or its name to `user_evaluate_fn`.
      - **For now, let's simplify:** Assume `program_code_string` defines a specific callable (e.g., a function named `solution_function_to_evaluate`) that the `user_evaluate_fn` knows how to call, or that `user_evaluate_fn` itself knows how to handle the raw `program_code_string`. The `EvaluationWrapper.run_evaluation` (from `task_utils.py`) provides a basic way to call `user_evaluate_fn`.
      - So, this method should primarily focus on setting up the call to `user_evaluate_fn` with the `program_code_string` (or a representation of it).
      - **Crucially, it needs to handle errors from the execution of `program_code_string` OR `user_evaluate_fn`.** This includes `SyntaxError` if `program_code_string` is invalid, `TimeoutError` (conceptual for now, actual implementation is advanced), and any runtime errors from the executed code or evaluation function.
      - If an error occurs, it should return a special score dictionary indicating failure (e.g., `{'error': True, 'score': -float('inf')}`).
      - If successful, it returns the dictionary of scores from `user_evaluate_fn`.
      - **Security Note (for user, not for Claude to fully implement now):** Executing arbitrary code is dangerous. In a real system, this would need robust sandboxing (e.g., Docker containers, gVisor, restricted interpreters). For now, we'll proceed with `exec` or `importlib` acknowledging this risk.
3. **(Conceptual for now - placeholders for advanced features):**
    - Add method signatures `_apply_evaluation_cascades(self, ...)` and `_get_llm_feedback(self, ...)` but have them `pass` or return mock data.
    - The `evaluate_program` method should be `async` to support future parallelization of many evaluations, even if a single evaluation is blocking for now.

Use the `EvaluationWrapper.run_evaluation` if it helps structure the call to the user's function.
Ensure clear docstrings and type hints.
