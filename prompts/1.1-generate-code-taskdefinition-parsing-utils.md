Hello Claude! We're beginning to build the AlphaEvolve system in Python.

**Role:** You are an expert Python developer.
**Task:** Implement initial components for task specification.

**File:** `alpha_evolve/task_utils.py`

**Request:**

1. Define a dataclass (or Pydantic model) named `TaskDefinition` with the following fields:
   - `problem_name: str`
   - `initial_code_path: str` (path to the user's initial Python code file or directory)
   - `evaluate_function_module_path: str` (path to the Python file containing the user's evaluation function)
   - `evaluate_function_name: str` (name of the evaluation function, e.g., 'evaluate')
2. Define a class `CodeParser` with a static method `extract_evolvable_blocks(code_string: str) -> list[tuple[str, str]]`:
   - This method should parse a given Python code string.
   - It should find blocks of code enclosed by `# EVOLVE-BLOCK-START <block_id>` and `# EVOLVE-BLOCK-END <block_id>` comments.
   - It should return a list of tuples, where each tuple is `(block_id, block_code_string)`. The `block_code_string` should be the content between the start and end markers, exclusive of the markers themselves.
   - If no blocks are found, it should return an empty list.
3. Define a class `EvaluationWrapper` with a method `load_user_evaluate_function(module_path: str, function_name: str) -> callable`:
   - This method should dynamically import the Python module from `module_path` and return the function object specified by `function_name`.
   - Handle potential `ImportError` or `AttributeError` if the module or function cannot be found, raising a custom informative error.
4. Add another method to `EvaluationWrapper` called `run_evaluation(self, program_module_or_string_to_test: any, user_evaluate_fn: callable, task_inputs: dict = None) -> dict[str, float]`:
   - This method will execute the `user_evaluate_fn`. For now, assume `user_evaluate_fn` can directly take `program_module_or_string_to_test` and optionally `task_inputs`.
   - It should call `user_evaluate_fn(program_module_or_string_to_test, **(task_inputs or {}))`
   - It must return a dictionary of scalar scores (e.g., `{'score1': 10.5}`).
   - Include basic error handling if `user_evaluate_fn` raises an exception during its execution.

Please ensure all classes and methods have clear docstrings, type hints, and follow PEP 8.
