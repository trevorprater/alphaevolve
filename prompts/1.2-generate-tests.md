Thanks, Claude! Here's the code for `alpha_evolve/task_utils.py` you generated:

{{ output from 1.1}}

**Role:** You are an expert Python test engineer.
**Task:** Write unit tests for the components in `task_utils.py` using `pytest`.

**File:** `tests/test_task_utils.py`

**Request:**
Please generate `pytest` unit tests covering:

1. **TaskDefinition:**
    - Initialization with valid data.
2. **CodeParser.extract_evolvable_blocks:**
    - Code with one evolvable block (check block_id and content).
    - Code with multiple evolvable blocks.
    - Code with no evolvable blocks.
    - Code with nested comments (ensure it doesn't break, though true nesting of blocks isn't supported by the spec).
    - Code with markers but no content in between.
3. **EvaluationWrapper.load_user_evaluate_function:**
    - Successfully loading a valid function from a dummy module (you'll need to create a temporary dummy module file for this test, perhaps using `tmp_path` fixture in pytest).
    - Attempting to load from a non-existent module (expect custom error).
    - Attempting to load a non-existent function from an existing module (expect custom error).
4. **EvaluationWrapper.run_evaluation:**
    - Running a dummy evaluation function that returns a valid score dict.
    - Running a dummy evaluation function that raises an exception (ensure your wrapper handles it).

Make sure to include necessary imports, mocks (if any, though direct calls are fine here), and fixtures like `tmp_path` for file operations.
