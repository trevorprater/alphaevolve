Thanks, Claude! Here's the code for `ProgramEntry` and `MAPElitesArchive` in `alpha_evolve/program_database.py`:

{{ paste output from 2.1 }}

**Role:** You are an expert Python test engineer.
**Task:** Write unit tests for `ProgramEntry` and `MAPElitesArchive` using `pytest`.

**File:** `tests/test_program_database.py` (you can append to this file or create a new one like `test_map_elites.py` if you prefer more granular test files for larger modules)

**Request:**
Generate `pytest` tests for:

1. **ProgramEntry:**
    - Initialization with all fields.
    - Initialization with `parent_id=None`.
2. **MAPElitesArchive:**
    - Initialization.
    - `_get_feature_bin_key`: Test with features mapping to valid bins, features out of bounds (lower and upper), features exactly on bin edges.
    - `add_program`:
      - Adding to an empty cell.
      - Adding a better program to an occupied cell (successful replacement).
      - Adding a worse or equal program to an occupied cell (no replacement).
      - Attempting to add a program whose features are out of defined bins (if `_get_feature_bin_key` returns None for this).
    - `get_elite`: Getting an existing elite, trying to get a non-existent one.
    - `get_random_elites`: Getting a specific number of elites, getting elites when fewer are available than requested, getting from an empty archive.

Use descriptive test names. Mock data for `ProgramEntry` instances.
