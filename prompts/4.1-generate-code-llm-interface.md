Hello Claude, next up is the `LLMInteractionModule`.

**Role:** You are an expert Python developer, familiar with interacting with external APIs.
**Task:** Implement an `LLMInterface` class to send prompts to code-generating LLMs.

**File:** `alpha_evolve/llm_interface.py`

**Request:**
Define the `LLMInterface` class:

- Its `__init__` could take configuration (e.g., API keys, model names for 'flash' and 'pro' type LLMs). For now, these can be placeholders.
- Implement an async method `async generate_code_modification(self, prompt: str, llm_type: str = 'flash') -> str`:
  - This method will be responsible for sending the `prompt` to an LLM.
  - Based on `llm_type` (e.g., 'flash' or 'pro'), it would theoretically select a different model or API endpoint.
  - **For now, do not implement actual API calls.** Instead, simulate the behavior:
    - If `llm_type` is 'flash', return a predefined mock diff string like: `"<<<<<<<< SEARCH\n# old code example\n========\n# new flash code example\n>>>>>>>> REPLACE"`.
    - If `llm_type` is 'pro', return a different mock diff string: `"<<<<<<<< SEARCH\n# old code example for pro\n========\n# new pro model suggestion\n>>>>>>>> REPLACE"`.
    - Add a small `asyncio.sleep(0.1)` to simulate network latency.
  - This method should be prepared to handle potential API errors in the future (e.g., by having try-except blocks, though they won't do much with the mock implementation).
- The method should return the LLM's response string (our mock diff).

Include docstrings, type hints, and make sure it's an `async` class if methods are async.
