Hello Claude, we're moving on to the `PromptSampler`.

**Role:** You are an expert Python developer.
**Task:** Implement the `PromptSampler` class which constructs prompts for LLMs.

**File:** `alpha_evolve/prompt_sampler.py`

**Request:**
Define the `PromptSampler` class:

- Its `__init__` should take an instance of `ProgramDatabase`.
- Implement `create_evolution_prompt(self, parent_program_ids: list[str], inspiration_program_ids: list[str], task_context: Optional[str] = None, desired_output_format: str = 'diff') -> str`:
  - Retrieve actual `ProgramEntry` objects for `parent_program_ids` and `inspiration_program_ids` from the `self.program_database`. Handle cases where IDs are not found.
  - Assemble a prompt string. This string should include:
    - A system instruction (e.g., "You are an expert coding assistant. Your task is to modify the given Python code of the 'parent program(s)' to improve its performance on metrics like [mention target metrics if known, or just 'defined metrics']. Use the 'inspiration program(s)' for ideas. Provide your changes in a diff format like so: <<<<<<<< SEARCH\n[original code]\n========\n[new code]\n>>>>>>>> REPLACE").
    - The code of the parent program(s). Clearly label them.
    - The code and scores of the inspiration program(s). Clearly label them.
    - If `task_context` is provided, include it.
    - Adjust instructions based on `desired_output_format` (e.g., if it's 'full_code', ask for the complete evolved block).
  - Return the fully assembled prompt string.
- Implement a helper `_format_program_for_prompt(self, program_entry: ProgramEntry, role: str) -> str` (e.g., role is "Parent Program" or "Inspiration Program") to format individual program details neatly for the prompt.

(Advanced features like stochastic formatting and meta-prompts from the blueprint will be skipped for this first pass).
Ensure clear docstrings and type hints.
