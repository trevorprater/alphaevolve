Thanks, Claude! Here's the updated `alpha_evolve/program_database.py` with the `ProgramDatabase` class:

{{ paste full content of alpha_evolve/program_database.py }}

**Role:** You are an expert Python test engineer.
**Task:** Write unit tests for the `ProgramDatabase` class using `pytest`.

**File:** `tests/test_program_database.py` (append to existing tests)

**Request:**
Generate `pytest` tests for the `ProgramDatabase` class:

1. Initialization (e.g., verify `MAPElitesArchive` is created).
2. `add_program`:
    - Ensure program is added to `all_programs_by_id`.
    - Ensure `map_elites_archive.add_program` is called (you might need to mock `MAPElitesArchive` or its `add_program` method to verify the call if direct state checking is hard, or simply check the size/content of the archive after adding).
3. `get_program_by_id`: Test retrieving an existing program and a non-existent one.
4. `sample_programs_for_prompting`:
    - Test it returns the correct number of parents and inspirations when enough elites are available in the (mocked or pre-populated) archive.
    - Test behavior when fewer elites are available than requested.

Use mocks for `MAPElitesArchive` if it simplifies testing the `ProgramDatabase`'s logic in isolation, or pre-populate a real archive for testing.
