Thanks, Claude! Here's the `DiffApplier` code from `alpha_evolve/diff_applier.py`:

{{Paste the DiffApplier code here}}

**Role:** You are an expert Python test engineer.
**Task:** Write unit tests for the `DiffApplier` class using `pytest`.

**File:** `tests/test_diff_applier.py`

**Request:**
Generate `pytest` unit tests for `DiffApplier`:

1. **Custom Exception:** If `DiffApplicationError` was defined, ensure it can be raised and caught.
2. **apply_diff:**
    - Successful application of a valid diff to a simple parent string.
    - Diff where the `SEARCH` block appears multiple times (ensure only the first is replaced).
    - Diff where the `SEARCH` block is not found in the parent string (expect `DiffApplicationError`).
    - Diff string with invalid format (e.g., missing `========` or `>>>>>>>> REPLACE`) (expect error or specific handling).
    - Diff with empty `SEARCH` block or empty `REPLACE` block.
    - Test with multi-line `SEARCH` and `REPLACE` blocks.
3. **apply_full_block_replace:**
    - Successful replacement of content within a specified `# EVOLVE-BLOCK-START/END id`.
    - Attempting to replace a block with an `id` that doesn't exist in the parent string (expect error).
    - Parent string with multiple evolvable blocks; ensure the correct one is targeted.
    - Replacing an empty block.
    - Replacing content with an empty `new_block_code_string`.

Use a variety of string inputs to cover edge cases.
