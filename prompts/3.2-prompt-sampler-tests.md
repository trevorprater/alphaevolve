Thanks, Claude! Here's the `PromptSampler` code from `alpha_evolve/prompt_sampler.py`:

{{ paste content of alpha_evolve/prompt_sampler.py }}

And here's a reminder of the `ProgramEntry` structure from `alpha_evolve/program_database.py`:

{{Paste ProgramEntry dataclass definition here}}

**Role:** You are an expert Python test engineer.
**Task:** Write unit tests for the `PromptSampler` class using `pytest`.

**File:** `tests/test_prompt_sampler.py`

**Request:**
Generate `pytest` tests for `PromptSampler`:

1. Initialization with a mock `ProgramDatabase`.
2. `create_evolution_prompt`:
   - Test with valid parent and inspiration program IDs. Mock the `program_database.get_program_by_id` method to return predefined `ProgramEntry` objects.
   - Verify that the generated prompt string contains the code and scores of these mock programs.
   - Verify that the system instruction and diff format instructions are present.
   - Test with `desired_output_format='full_code'` and check if instructions change.
   - Test behavior when a program ID is not found in the mock database (e.g., it should handle this gracefully, perhaps by raising an error or logging and skipping).
3. `_format_program_for_prompt`: Test this helper directly to ensure correct formatting.

Use a `MagicMock` for `ProgramDatabase`.
